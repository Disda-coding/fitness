{
  "name": "fitness-tracker",
  "version": "1.1.1",
  "private": true,
  "dependencies": {
    "hono": "^4.8.5"
  },
  "scripts": {
    "deploy:frontend": "node scripts/deploy-frontend.js",
    "deploy:backend": "wrangler deploy",
    "migrate:d1": "wrangler d1 execute fitness-data --remote --file=migrations/003_add_indexes.sql"
  }
}
